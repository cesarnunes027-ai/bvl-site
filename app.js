async function loadProducts(){const products=[{name:'VIP Bronze',price:10},{name:'VIP Prata',price:20},{name:'VIP Ouro',price:35},{name:'VIP Diamante',price:50},{name:'Level +5',price:7},{name:'Estudos +50',price:5},{name:'Gold 10.000',price:15}];const c=document.getElementById('products');products.forEach(p=>{const div=document.createElement('div');div.className='product';div.innerHTML=`<h3>${p.name}</h3><p>R$ ${p.price}</p><button onclick="pay('${p.name}',${p.price})">Comprar</button>`;c.appendChild(div);});}async function pay(name,price){const r=await fetch('/api/create_preference',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,price})});const d=await r.json();window.location=d.init_point;}loadProducts();